{
  "$schema": "http://json-schema.org/schema",
  "$id": "Gitlint",
  "title": "Gitlint Schema",
  "description": "Adhere the workspace to a git commit convention.",
  "type": "object",
  "properties": {
    "gitflow": {
      "type": "boolean",
      "default": true,
      "description": "Configure git-flow in the project.",
      "x-priority": "important",
      "x-prompt": "Make sure git-flow is installed on the system!"
    },
    "appScopes": {
      "type": "boolean",
      "default": true,
      "description": "Add existing applications as commit scopes."
    },
    "libScopes": {
      "type": "boolean",
      "default": true,
      "description": "Add existing libraries as commit scopes."
    },
    "scopes": {
      "type": "string",
      "description": "A comma-separated list of custom commit scopes."
    },
    "skipFormat": {
      "description": "Skip formatting files.",
      "type": "boolean",
      "default": false,
      "x-priority": "internal"
    }
  },
  "required": []
}
